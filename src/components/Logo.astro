---
import {config} from "../config/config.ts";

const companyLogo = config.companyLogo;
---

<a href="/" class="flex-shrink-0 flex items-center">
  <img class="h-8 w-auto" src={companyLogo.path} alt={companyLogo.alt} />
</a>
